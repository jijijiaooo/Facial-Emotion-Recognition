# Flask API Requirements
# Core dependencies for running the emotion detection API

# Web Framework
Flask==3.0.0
flask-cors==4.0.0
gunicorn==21.2.0

# Image Processing
opencv-python-headless==4.8.1.78
Pillow==10.1.0
numpy<2.0

# Machine Learning - Updated to Keras 3 compatible version
tensorflow==2.16.1
keras==3.3.3

# Facial Landmarks (optional - will use fallback if not available)
# dlib==19.24.2  # Commented out - difficult to build on Render, using fallback mode
