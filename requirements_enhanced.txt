# Enhanced CNN Training Requirements
# Install these packages for the enhanced training script with facial landmarks and action units

# Core ML and Image Processing
numpy>=1.21.0
opencv-python>=4.5.0
tensorflow>=2.10.0
keras>=2.10.0
Pillow>=8.0.0

# Facial Landmark Detection (REQUIRED for enhanced features)
dlib>=19.22.0
# Note: dlib requires cmake and boost. Install system dependencies first:
# macOS: brew install cmake
# Ubuntu: sudo apt-get install cmake libboost-all-dev

# Advanced Image Augmentation (RECOMMENDED)
albumentations>=1.3.0
# Albumentations provides superior augmentation compared to basic transforms

# Additional ML utilities
scikit-learn>=1.3.0

# Progress and visualization (optional)
tqdm>=4.65.0
matplotlib>=3.5.0
seaborn>=0.12.0

# TensorBoard (for training visualization)
tensorboard>=2.10.0

# Model conversion (optional)
tflite-runtime

# For evaluation and metrics
pandas>=1.5.0
